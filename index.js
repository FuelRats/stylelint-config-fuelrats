module.exports = {
  extends: [
    './rules/errors',
    './rules/limitfeatures',
    './rules/style',
    './rules/plugin-scss',
    // Disabled until we get a hold on everything else,
    // './rules/plugin-order',
  ],
}
